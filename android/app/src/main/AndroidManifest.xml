<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 🎯 آڈیو ریکارڈنگ اور مائیکروفون کی پرمیشنز -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <!-- 🟢 اینڈرائیڈ 13 اور جدید ورژنز پر آڈیو فائلز ریڈ کرنے کے لیے -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- 🎯 فنگر پرنٹ / بائیو میٹرک کے لیے ضروری پرمیشن -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- 🟢 انٹرنیٹ اور فون کال لنکس (url_launcher) کی پرمیشنز -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="nayab_qist_point_customer"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- 🟢 url_launcher سے کالنگ اسکیم سنبھالنے کے لیے -->
        <intent>
            <action android:name="android.intent.action.DIAL" />
            <data android:scheme="tel" />
        </intent>
    </queries>
</manifest>